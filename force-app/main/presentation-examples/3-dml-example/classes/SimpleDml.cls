public with sharing class SimpleDml {
    //
    // Obviously, this is a trivial example, but it's possible that you could
    // have a utility Dml class that simplifies logging / ading errors to related
    // records and suchlike
    //
    public void upsertRecords( List<Sobject> objectsToUpsert ) {
        upsertRecords( objectsToUpsert );
    }
}